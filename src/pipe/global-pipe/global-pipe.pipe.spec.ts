import { GlobalPipePipe } from './global-pipe.pipe';

describe('GlobalPipePipe', () => {
  it('create an instance', () => {
    const pipe = new GlobalPipePipe();
    expect(pipe).toBeTruthy();
  });
});
